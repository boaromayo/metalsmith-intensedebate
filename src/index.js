/*
 * Plugin for Metalsmith to allow intensedebate comments.
 * @author boaromayo
 */

const defaultvars = {
	url: '',
	acct_id: '000000000',
	shortname: 'ocamposmoon',
	post_id: '000000000',
	counter: '.counter'
};

const counters = function() {
	
};

/*
 * Plugin for Metalsmith to allow intensedebate comments.
 * @return {Function}
 */
const plugin = function() {
	return(files, Metalsmith, done) = function() {


		done();
	}
};

module.exports = plugin;